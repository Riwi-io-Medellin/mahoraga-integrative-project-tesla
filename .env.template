# Copy this file to `.env` in the project root.
# The backend reads these variables from the root `.env`.

# Express server port
APP_PORT=3000

# PostgreSQL connection used by src/config/db.config.js
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mahoraga_db
DB_USER=postgres
DB_PWD=postgres
